CREATE TABLE Kanji(
name TEXT NOT NULL PRIMARY KEY,
craft TEXT NOT NULL,
personImageUrl TEXT,
personBio TEXT
);

insertItem:
INSERT OR REPLACE INTO Kanji(name, craft, personImageUrl, personBio)VALUES(?,?,?,?);

selectAll:
SELECT * FROM Kanji;

deleteAll:
DELETE FROM Kanji;